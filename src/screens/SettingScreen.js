import React from 'react';
import { StyleSheet, View, Image, Pressable } from 'react-native';

const SettingsScreen = () => {
    return (
        <View>
            <Text styles={{flex:1}}>SettingsScreen</Text>
        </View>
    );
};

const styles = StyleSheet.create();

export default SettingsScreen;