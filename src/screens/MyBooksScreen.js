import React from 'react';
import { StyleSheet, View, Text, Image, Pressable } from 'react-native';

const MyBookScreen = () => {
    return (
        <View>
            <Text styles={{flex:1}}>MyBookScreen</Text>
        </View>
    );
};

const styles = StyleSheet.create();

export default MyBookScreen;