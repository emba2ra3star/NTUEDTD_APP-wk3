import React from 'react';
import { StyleSheet, View, Image, Pressable } from 'react-native';

const AccountScreen = () => {
    return (
        <View>
            <Text styles={{flex:1}}>AccountScreen</Text>
        </View>
    );
};

const styles = StyleSheet.create();

export default AccountScreen;